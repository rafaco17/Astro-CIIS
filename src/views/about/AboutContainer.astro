---
import Card from "../about/components/Card.astro";
import aboutCIIS from "../about/services/aboutService";
import type Props from "./adapters/service";
---

<section id="about" class="bg-[url(../../../public/fondo.gif)] bg-cover overflow-hidden relative text-white pt-[40px] pb-[26px] text-center box-shadow-0px-0px-65px-[#000820] bg-fixed before:content-[''] before:bg-[#000820cc] before:absolute before:top-0 before:left-0 before:right-0 before:bottom-0">
    <div class="w-full px-4 mx-auto">
        <div class="flex flex-wrap mx-[-15px] justify-center align-middle gap-10">
            {
                aboutCIIS.map((item) => (
                    <Card title1={item.title1} title2={item.title2} description={item.description} />
                ))
            }
        </div>
    </div>  
</section>
